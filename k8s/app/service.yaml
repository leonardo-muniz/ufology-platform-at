apiVersion: v1
kind: Service
metadata:
  name: ufotracker
  namespace: ufology
  labels:
    app: ufotracker
spec:
  type: ClusterIP
  selector:
    app: ufotracker
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080